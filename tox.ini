[tox]
envlist = py37,py38,py39,py310

[testenv]
skip_install = True
passenv =
    PYTEST_ADDOPTS
deps =
    flake8
    mintotp
    pytest
commands =
    flake8 twofa.py tests/
    pytest {posargs:tests/}
